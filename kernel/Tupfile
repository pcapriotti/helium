include_rules

CFLAGS += $(CFLAGS_HELIUM)

CC=$HOME/.local/opt/cross/bin/$(TARGET)-gcc
LD=$HOME/.local/opt/cross/bin/$(TARGET)-ld

LIBS=../libc/libc.a ../buddy/buddy.a ../ext2/ext2.a ../core/core.a
LSCRIPT=kernel.ld
LDFLAGS += -e kmain

: foreach *.S |> !as |>
: foreach *.c |> !cc |>
: *.o $(LIBS) |> !ld |> kernel.elf
