include_rules

CC=gcc

CFLAGS += -g -I..

# recompile buddy library for the host
: foreach ../buddy/*.c |> ^ CC %f^ $(CC) $(CFLAGS) -c %f -o %o |> buddy/%B.o

: foreach *.c |> !cc |>
: *.o buddy/*.o |> ^ LINK %o^ $(CC) $(LDFLAGS) %f -o %o |> test
